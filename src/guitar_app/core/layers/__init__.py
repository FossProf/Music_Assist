"""Fretboard overlay layers.

A layer is a toggleable projection of a musical concept onto a fretboard. Layers
compute structured annotations and never render; the UI consumes their results
and owns all drawing.
"""
